function roundingANumber(number1,number2){
if(typeof (number1&&number2)==="number"&&Number.isNaN(number1&&number2)!=="NaN"){
const result=number1+number2;
 }

   return result.toFixed(3);
}

module.exports=roundingANumber